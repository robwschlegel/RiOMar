#!/usr/bin/env python3
# -*- coding: utf-8 -*-

# Placeholder -- kept for git history. The driver-vs-plume X11 comparison
# figures (figures/driver_x11_comparison/Figure6_style_<driver>.png) are now
# plotted entirely in R -- see func/generate_driver_x11_comparison_figures.R
# and func/X11.R::plot_driver_x11_trend_comparison() -- per Robert's call
# (2026-09-23) that Python stays limited to the X11 *calculation*
# (func/compute_x11_driver_signals.py) and R does all the plotting, matching
# river flow's own Fig 6/7 convention. Section 2 of
# func/compute_driver_x11_figures.R (the raw-daily wind/wave category-split
# plots) was never Python and is unaffected -- still run via
# Rscript func/compute_driver_x11_figures.R as before.
